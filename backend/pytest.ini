[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live: integration tests against a real Ollama server (set AGENTFORGE_LIVE_TESTS=1)
    audit: mandatory command audit logging tests
    quality: prompt-to-outcome and orchestration quality tests
